% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration-tidytransit.R
\name{download_best_feed}
\alias{download_best_feed}
\title{Download the "best" GTFS Schedule feed with smart selection}
\usage{
download_best_feed(
  provider = NULL,
  country_code = NULL,
  subdivision_name = NULL,
  municipality = NULL,
  feed_name = NULL,
  prefer_official = TRUE,
  prefer_active = TRUE,
  max_validation_errors = NULL,
  interactive = NULL,
  exclude_flex = TRUE,
  use_source_url = FALSE,
  auth_args = NULL,
  export_path = NULL,
  raw = NULL,
  ...
)
}
\arguments{
\item{provider}{Provider/agency name (partial match).}

\item{country_code}{ISO 2-letter country code (requires \code{subdivision_name}).}

\item{subdivision_name}{State/province/region name (requires \code{country_code}).}

\item{municipality}{City name.}

\item{feed_name}{Feed name filter (case-insensitive substring match).}

\item{prefer_official}{Logical. If \code{TRUE} (default), prefer feeds marked as official.}

\item{prefer_active}{Logical. If \code{TRUE} (default), prefer feeds with status "active"
over "future", "development", "deprecated", or "inactive".}

\item{max_validation_errors}{Integer. Maximum number of validation errors allowed.
Feeds exceeding this threshold are filtered out. If \code{NULL} (default), no filtering.}

\item{interactive}{Logical. If \code{TRUE}, prompt user to select when multiple equally-ranked
feeds exist. If \code{FALSE}, automatically select the first highest-ranked feed with a
warning. If \code{NULL} (default), uses \code{getOption("mobdb.interactive")} or falls back to
\code{interactive()} to detect if running in an interactive R session.}

\item{exclude_flex}{Logical. If \code{TRUE} (default), exclude GTFS-Flex feeds.}

\item{use_source_url}{Logical. Download from agency's source URL (\code{TRUE}) or
Mobility Database's hosted URL (\code{FALSE}, default).}

\item{auth_args}{Authentication arguments if required (see \code{\link[=download_feed]{download_feed()}}).}

\item{export_path}{A string. Optional path to save the GTFS feed as a ZIP file
(e.g., "data/gtfs/feed.zip"). See \code{\link[=download_feed]{download_feed()}} for details.}

\item{raw}{A logical. Controls whether the file saved to \code{export_path} is the
raw download (\code{TRUE}) or a parsed-and-re-exported version (\code{FALSE}).
Defaults to \code{TRUE} when \code{export_path} is provided, \code{FALSE} otherwise.}

\item{...}{Additional arguments passed to \code{\link[tidytransit:read_gtfs]{tidytransit::read_gtfs()}}.}
}
\value{
If \code{export_path} is provided with \code{raw = TRUE} (the default when
exporting), the file path (invisibly). Otherwise, a \code{gtfs} object from
tidytransit, or \code{NULL} if user cancels selection.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A wrapper around \code{\link[=download_feed]{download_feed()}} that \emph{automagically} selects
the best GTFS Schedule feed when multiple options exist. This function:
\itemize{
\item Searches for feeds using provider name or location
\item Ranks feeds by status, official designation, and validation quality
\item Prompts for user selection when multiple equally-ranked feeds exist (in interactive mode)
\item Falls back to historical datasets when current feed is marked "future" or "inactive"
\item Only works with GTFS Schedule feeds (not GTFS-RT or GBFS)
}

This is designed for use cases where you just want the best, most recent feed
without needing to specify exact feed IDs or handle multiple results manually.
}
\section{Selection algorithm}{

When multiple feeds match the search criteria, feeds are ranked by:
\enumerate{
\item \strong{Status} (if \code{prefer_active = TRUE}): active > future > development > inactive > deprecated
\item \strong{Official designation} (if \code{prefer_official = TRUE}): official > unclassified > unofficial
\item \strong{Validation quality}: Feeds with fewer errors score higher
\item \strong{Service date coverage}: Feeds covering today's date score higher
\item \strong{Recency}: More recently added feeds get a tiebreaker boost
}

If multiple feeds have the same score and \code{interactive = TRUE}, you'll be prompted to choose.
}

\section{Status handling}{

The function handles different feed statuses as follows:
\itemize{
\item \strong{"active"}: Preferred. Feed should be used in public trip planners.
\item \strong{"future"} or \strong{"inactive"}: Automatically searches for historical datasets with
service dates covering today. "future" feeds are not yet active; "inactive" feeds
haven't been recently updated and may provide outdated information.
\item \strong{"deprecated"}: Explicitly deprecated and shouldn't be used. Warns user to search
for a replacement feed.
\item \strong{"development"}: For development purposes only, shouldn't be used in production.
}
}

\section{GTFS Schedule Only}{

Like \code{\link[=download_feed]{download_feed()}}, this function only works with GTFS Schedule feeds.
For GTFS-RT or GBFS feeds, use \code{\link[=mobdb_read_gtfs]{mobdb_read_gtfs()}} or fetch URLs with \code{\link[=mobdb_get_feed]{mobdb_get_feed()}}.
}

\examples{
\dontshow{if (mobdb_can_run_examples() && mobdb_has_tidytransit()) withAutoprint(\{ # examplesIf}
# Simple one-shot download by provider name
bart_feed <- download_best_feed(provider = "Bay Area Rapid Transit")


# Non-interactive mode (for scripts)
options(mobdb.interactive = FALSE)
feed <- download_best_feed(provider = "Bay Area Rapid Transit")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=download_feed]{download_feed()}} for precise control,
\code{\link[=feeds]{feeds()}} to explore available feeds before downloading,
\code{\link[=mobdb_search]{mobdb_search()}} for full-text search with validation data
}
