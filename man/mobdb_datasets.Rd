% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-datasets.R
\name{mobdb_datasets}
\alias{mobdb_datasets}
\title{Get datasets for a feed}
\usage{
mobdb_datasets(feed_id, latest = TRUE, use_cache = TRUE)
}
\arguments{
\item{feed_id}{A string. The unique identifier for the feed.}

\item{latest}{A logical. If \code{TRUE} (default), return only the most recent dataset.
If \code{FALSE}, return all available datasets.}

\item{use_cache}{A logical. If \code{TRUE} (default), use cached results if available.
If \code{FALSE}, always fetch fresh data from the API. Cached data expires after 24 hours
(datasets are immutable).}
}
\value{
A tibble containing dataset information including:
\itemize{
\item \code{id} - Dataset identifier
\item \code{feed_id} - Associated feed ID
\item \code{downloaded_at} - Timestamp when dataset was captured
\item \code{hash} - Hash of the dataset file
\item \code{download_url} - URL to download this specific dataset version
\item Additional metadata columns
}
}
\description{
Retrieve information about available datasets (historical versions) for
a specific feed. Each dataset represents a snapshot of the feed at a
particular point in time.
}
\examples{
\dontrun{
# Get latest dataset for a feed (GTFS schedule feeds only)
latest <- mobdb_datasets("mdb-53")

# Get all historical datasets
all_versions <- mobdb_datasets("mdb-53", latest = FALSE)
}

}
\seealso{
\code{\link[=download_feed]{download_feed()}} to download specific historical versions,
\code{\link[=get_validation_report]{get_validation_report()}} to extract validation data from datasets,
\code{\link[=mobdb_get_dataset]{mobdb_get_dataset()}} to get details for a specific dataset
}
\concept{datasets}
