% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-feeds.R
\name{mobdb_feed_url}
\alias{mobdb_feed_url}
\title{Get download URL for a feed}
\usage{
mobdb_feed_url(feed_id)
}
\arguments{
\item{feed_id}{A string. The unique identifier for the feed.}
}
\value{
A string. The direct download URL, or \code{NULL} if not available.
}
\description{
Convenience function to quickly get the direct download or source URL for a feed.
This is useful for passing to tidytransit::read_gtfs() or similar
functions.
}
\examples{
\dontshow{if (mobdb_can_run_examples()) withAutoprint(\{ # examplesIf}
# Get download URL
url <- mobdb_feed_url("mdb-53")
\dontshow{\}) # examplesIf}
\dontshow{if (mobdb_can_run_examples() && mobdb_has_tidytransit()) withAutoprint(\{ # examplesIf}
# Use with tidytransit
library(tidytransit)
gtfs <- read_gtfs(url)
\dontshow{\}) # examplesIf}
}
