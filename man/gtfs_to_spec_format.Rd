% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-gtfs-format.R
\name{gtfs_to_spec_format}
\alias{gtfs_to_spec_format}
\title{Convert tidygtfs object to GTFS-spec-compliant format}
\usage{
gtfs_to_spec_format(gtfs)
}
\arguments{
\item{gtfs}{A gtfs/tidygtfs object, typically from \code{\link[tidytransit:read_gtfs]{tidytransit::read_gtfs()}}
or \code{\link[=download_feed]{download_feed()}}.}
}
\value{
A modified copy of the gtfs object with date and time columns
converted to GTFS-spec-compliant strings.
}
\description{
Converts a tidygtfs object (as returned by \code{\link[tidytransit:read_gtfs]{tidytransit::read_gtfs()}})
back to GTFS-spec-compliant string formats. This reverses tidytransit's
automatic type conversions:
\itemize{
\item \strong{Date columns} (R \code{Date} objects) are converted back to YYYYMMDD strings
(e.g., \code{as.Date("2024-01-15")} becomes \code{"20240115"})
\item \strong{Time columns} (\code{hms}/\code{difftime} objects) are converted back to HH:MM:SS
strings, preserving values >= 24:00:00 for trips past midnight
(e.g., \code{hms::hms(hours = 25, minutes = 30)} becomes \code{"25:30:00"})
}

Columns that are already in the correct format (character or integer) are
left unchanged. Returns a modified copy; the original object is not modified.
}
\section{Affected tables and columns}{

\strong{Date columns} (YYYYMMDD):
\itemize{
\item \code{calendar}: \code{start_date}, \code{end_date}
\item \code{calendar_dates}: \code{date}
\item \code{feed_info}: \code{feed_start_date}, \code{feed_end_date}
}

\strong{Time columns} (HH:MM:SS):
\itemize{
\item \code{stop_times}: \code{arrival_time}, \code{departure_time}
\item \code{frequencies}: \code{start_time}, \code{end_time}
}
}

\examples{
\dontshow{if (mobdb_can_run_examples() && mobdb_has_tidytransit()) withAutoprint(\{ # examplesIf}
gtfs <- download_feed("mdb-247")

# Dates are R Date objects from tidytransit
class(gtfs$calendar$start_date)
# [1] "Date"

# Convert to GTFS-spec format
spec <- gtfs_to_spec_format(gtfs)
spec$calendar$start_date
# [1] "20240101"

spec$stop_times$arrival_time[1]
# [1] "08:30:00"
\dontshow{\}) # examplesIf}
}
