% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-search.R
\name{mobdb_search}
\alias{mobdb_search}
\title{Search for feeds across the Mobility Database}
\usage{
mobdb_search(query, data_type = NULL, country_code = NULL, limit = 50)
}
\arguments{
\item{query}{Character. Search query string. Searches across provider names,
feed names, and locations.}

\item{data_type}{Character. Optional filter by data type: "gtfs" or "gtfs_rt".}

\item{country_code}{Character. Optional filter by two-letter ISO country code.}

\item{limit}{Integer. Maximum number of results (default: 50).}
}
\value{
A tibble of matching feeds. Note that search results include additional
fields compared to \code{\link[=mobdb_feeds]{mobdb_feeds()}}:
\itemize{
\item \code{locations} - List of data frames with geographical information
\item \code{latest_dataset} - Data frame with most recent dataset details and validation
\item Core fields (\code{id}, \code{provider}, \code{data_type}, \code{status}, \code{source_info}) are the same
}
}
\description{
Perform a text search across feed names, providers, and locations.
This is useful when you don't know the exact feed ID or want to
discover feeds based on keywords.
}
\examples{
\dontrun{
# Search for transit agencies
bart <- mobdb_search("BART")
mta <- mobdb_search("MTA New York")

# Search with filters
canadian_transit <- mobdb_search(
  "transit",
  country_code = "CA",
  data_type = "gtfs"
)
}

}
