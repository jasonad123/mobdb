% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{mobdb_extract_locations}
\alias{mobdb_extract_locations}
\title{Extract location information from search results}
\usage{
mobdb_extract_locations(results, unnest = TRUE)
}
\arguments{
\item{results}{A tibble returned by \code{\link[=mobdb_search]{mobdb_search()}}.}

\item{unnest}{Logical. If \code{TRUE} (default), unnest locations so each feed-location
combination gets its own row. If \code{FALSE}, return a summary of locations per feed.}
}
\value{
A tibble with location information. If \code{unnest = TRUE}, each row represents
a feed-location pair. If \code{unnest = FALSE}, returns one row per feed with
concatenated location strings.
}
\description{
Helper function to extract and unnest location information from search results.
The \code{locations} field in search results is a list of data frames; this function
flattens it into a more usable format.
}
\examples{
# Create sample data matching mobdb_search() output structure
sample_results <- tibble::tibble(
  id = c("mdb-1", "mdb-2"),
  provider = c("Agency A", "Agency B"),
  locations = list(
    data.frame(
      country_code = "US",
      country = "United States",
      subdivision_name = "California",
      municipality = "San Francisco"
    ),
    data.frame(
      country_code = "CA",
      country = "Canada",
      subdivision_name = "British Columbia",
      municipality = "Vancouver"
    )
  )
)

# Extract and unnest locations
mobdb_extract_locations(sample_results)

# Get summary without unnesting
mobdb_extract_locations(sample_results, unnest = FALSE)

\dontshow{if (mobdb_can_run_examples()) withAutoprint(\{ # examplesIf}
# With real API data:
results <- mobdb_search("California")
locations <- mobdb_extract_locations(results)
\dontshow{\}) # examplesIf}
}
