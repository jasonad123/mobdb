% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration-tidytransit.R
\name{download_feed}
\alias{download_feed}
\title{Download GTFS Schedule feeds}
\usage{
download_feed(
  feed_id = NULL,
  provider = NULL,
  country_code = NULL,
  subdivision_name = NULL,
  municipality = NULL,
  exclude_flex = TRUE,
  feed_name = NULL,
  use_source_url = FALSE,
  dataset_id = NULL,
  latest = TRUE,
  status = "active",
  official = NULL,
  auth_args = NULL,
  export_path = NULL,
  raw = NULL,
  ...
)
}
\arguments{
\item{feed_id}{A string or data frame. The unique identifier for the feed
(e.g., "mdb-2862"), or a single-row data frame from \code{\link[=feeds]{feeds()}} or
\code{\link[=mobdb_search]{mobdb_search()}}. If a data frame is provided, the feed ID will be extracted
automatically. If provided, all other search parameters are ignored.}

\item{provider}{A string. Filter by provider/agency name (partial match).
Use this to search for feeds without knowing the feed_id.}

\item{country_code}{A string. Two-letter ISO country code (e.g., "US", "CA").}

\item{subdivision_name}{A string. State, province, or region name.}

\item{municipality}{A string. City or municipality name.}

\item{exclude_flex}{A logical. If \code{TRUE} (default), automatically exclude feeds
with "flex" in the feed name (case-insensitive). GTFS-Flex feeds are an extension of
the GTFS Schedule specification and may contain files that have unique schemas
that may not work with standard GTFS tools.}

\item{feed_name}{A string. Optional filter for feed name. If provided, only
feeds whose \code{feed_name} contains this string (case-insensitive) will be
considered. Use \code{NULL} (default) to skip this filter.}

\item{use_source_url}{A logical. If \code{FALSE} (default), uses Mobility Database's
hosted/archived URL which ensures you get the exact version in their database.
If \code{TRUE}, uses the provider's direct source URL which may be more current
but could differ from the hosted version.}

\item{dataset_id}{A string. Optional specific dataset ID for historical versions
(e.g., "mdb-53-202510250025"). If provided, downloads that specific dataset
version instead of the latest. Cannot be used with \code{use_source_url = TRUE}.
If \code{dataset_id} is provided without \code{feed_id}, the feed ID will be automatically
extracted from the dataset ID format.}

\item{latest}{A logical. If \code{TRUE} (default), download the most recent dataset.
If \code{FALSE}, returns information about all available datasets for the feed
without downloading. Only works when \code{feed_id} is provided directly; cannot
be used with search parameters like \code{provider} or \code{country_code}.}

\item{status}{A string. Feed status filter: "active" (default), "deprecated",
"inactive", "development", or "future". Only used when searching by provider/location.}

\item{official}{A logical. If \code{NULL} (default), return all feeds regardless
of official status when searching by provider/location. If \code{TRUE}, return
official feeds and feeds with unknown official status (NA). If \code{FALSE},
only return feeds explicitly marked as unofficial.}

\item{auth_args}{A string. Some agencies require authentication to download
feeds directly from their source URLs. Provide your API key/token in one of
two formats:
\itemize{
\item Just the value: \code{"your_api_key_here"}
\item Parameter and value: \code{"apikey=your_api_key_here"}
}

Also accepts a value stored in \code{.Renviron} (.e.g Sys.getenv("AGENCY_API_KEY") stored in the same formats)
Only valid when \code{use_source_url = TRUE}. If a feed requires authentication, you'll receive an error message with a link to obtain credentials.
The authentication method (URL parameter or HTTP header) is determined
automatically from the feed's metadata.}

\item{export_path}{A string. Optional path to save the GTFS feed as a ZIP file
(e.g., "data/gtfs/feed.zip"). By default, saves the raw file exactly as
downloaded. Set \code{raw = FALSE} to parse with tidytransit and re-export in
GTFS-spec-compliant format (requires \code{tidytransit} and \code{gtfsio}).
If \code{NULL} (default), the feed is not saved to disk.}

\item{raw}{A logical. Controls whether the file saved to \code{export_path} is the
raw download (\code{TRUE}) or a parsed-and-re-exported version (\code{FALSE}).
Defaults to \code{TRUE} when \code{export_path} is provided, \code{FALSE} otherwise.}

\item{...}{Additional arguments passed to \code{\link[tidytransit:read_gtfs]{tidytransit::read_gtfs()}}.}
}
\value{
If \code{export_path} is provided with \code{raw = TRUE} (the default when
exporting), the file path (invisibly). If \code{latest = TRUE}, a \code{gtfs} object
as returned by \code{\link[tidytransit:read_gtfs]{tidytransit::read_gtfs()}}.
If \code{latest = FALSE}, a tibble of all available datasets with their metadata.
}
\description{
A convenience function for downloading GTFS Schedule feeds from the Mobility Database.
This is a "one-stop-shop" that can search for feeds by provider/location and download
them in a single call, or download a specific feed by ID.

\strong{Note:} This function is specifically designed for GTFS Schedule feeds only.
GTFS Realtime and GBFS feeds use a different data model and are not supported by this function.

\emph{This function was formerly called \code{mobdb_download_feed()}.}
}
\examples{
\dontshow{if (mobdb_can_run_examples() && mobdb_has_tidytransit()) withAutoprint(\{ # examplesIf}
# Download by feed ID
gtfs <- download_feed("mdb-2862")

# Download from search results
feeds <- feeds(provider = "TransLink", data_type = "gtfs")
gtfs <- download_feed(feeds[1, ])

# Search and download by provider name
gtfs <- download_feed(provider = "Arlington")

# Download using agency's source URL instead of Mobility Database
gtfs <- download_feed(provider = "TriMet", use_source_url = TRUE)

# See all available versions for a feed
versions <- download_feed("mdb-2862", latest = FALSE)

# Download a specific historical version (feed_id auto-extracted from dataset_id)
historical <- download_feed(dataset_id = "mdb-53-202507240047")

# Filter by location (may return multiple feeds requiring disambiguation,
# in which case refine with `provider` or `feed_name`)
try(download_feed(
  country_code = "US",
  subdivision_name = "California",
  municipality = "San Francisco"
))

# Save GTFS feed to disk (raw file, no parsing required)
path <- download_feed("mdb-247", export_path = tempfile(fileext = ".zip"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mobdb_datasets]{mobdb_datasets()}} to list all available historical versions,
\code{\link[=get_validation_report]{get_validation_report()}} to check feed quality before downloading,
\code{\link[=feeds]{feeds()}} to search for feeds,
\code{\link[=mobdb_read_gtfs]{mobdb_read_gtfs()}} for more flexible GTFS reading
}
