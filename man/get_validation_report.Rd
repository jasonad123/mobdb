% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{get_validation_report}
\alias{get_validation_report}
\title{Get validation report for feeds or datasets}
\usage{
get_validation_report(data)
}
\arguments{
\item{data}{A tibble from \code{\link[=feeds]{feeds()}}, \code{\link[=mobdb_datasets]{mobdb_datasets()}}, or \code{\link[=mobdb_search]{mobdb_search()}}.}
}
\value{
A tibble with validation summary information:
\itemize{
\item \code{feed_id} or \code{dataset_id} - Identifier
\item \code{provider} - Provider name (if available)
\item \code{total_error} - Number of validation errors
\item \code{total_warning} - Number of validation warnings
\item \code{total_info} - Number of informational notices
\item \code{html_report} - URL to full HTML validation report
\item \code{json_report} - URL to JSON validation report
}
}
\description{
Extract validation report summary from feed/dataset results. MobilityData
runs all GTFS Schedule feeds through the canonical GTFS validator, and this
function surfaces that validation data to help assess feed quality before
downloading.
}
\examples{
\dontrun{
# Get validation report for feeds from search
bart_feeds <- feeds(provider = "Bay Area Rapid Transit")
datasets <- mobdb_datasets(bart_feeds$id[1])
validation <- get_validation_report(datasets)
print(validation)

# Check TransLink Vancouver's validation (has known warnings)
# Per TransLink's GTFS page "We pass our data through Google's Transit Feed
# Validator at the error level, but the data may have warnings left unfixed
# in order to conform to TransLink's business rules, such as duplicate stops
# with no distance between them."
vancouver <- feeds(provider = "TransLink", country_code = "CA", data_type = "gtfs")
vancouver_datasets <- mobdb_datasets(vancouver$id[1])
validation <- get_validation_report(vancouver_datasets)
# Shows: 100,076 errors, 14,322,543 warnings
}
}
\seealso{
\code{\link[=filter_by_validation]{filter_by_validation()}} to filter by quality thresholds,
\code{\link[=view_validation_report]{view_validation_report()}} to open full HTML/JSON reports in browser,
\code{\link[=mobdb_datasets]{mobdb_datasets()}} to get dataset information with validation data,
\code{\link[=mobdb_extract_datasets]{mobdb_extract_datasets()}} to extract validation from search results
}
\concept{validation}
