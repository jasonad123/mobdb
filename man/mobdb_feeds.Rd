% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-feeds.R
\name{mobdb_feeds}
\alias{mobdb_feeds}
\title{List and filter GTFS feeds}
\usage{
mobdb_feeds(
  provider = NULL,
  country_code = NULL,
  subdivision_name = NULL,
  municipality = NULL,
  data_type = NULL,
  status = NULL,
  limit = 100,
  offset = 0
)
}
\arguments{
\item{provider}{Character. Filter by provider/agency name (partial match).}

\item{country_code}{Character. Two-letter ISO country code (e.g., "US", "CA").}

\item{subdivision_name}{Character. State, province, or region name.}

\item{municipality}{Character. City or municipality name.}

\item{data_type}{Character. Type of feed: "gtfs" (schedule), "gtfs_rt" (realtime), or "gbfs" (bike share).}

\item{status}{Character. Feed status: "active", "inactive", or "deprecated".}

\item{limit}{Integer. Maximum number of results to return (default: 100).}

\item{offset}{Integer. Number of results to skip for pagination (default: 0).}
}
\value{
A tibble containing feed information with columns including:
\itemize{
\item \code{id} - Unique feed identifier
\item \code{provider} - Transit agency/provider name
\item \code{data_type} - Type of feed (gtfs or gtfs_rt)
\item \code{status} - Feed status (active, inactive, deprecated)
\item \code{source_info} - Data frame containing:
\itemize{
\item \code{producer_url} - Direct download URL for the feed
\item \code{authentication_type} - Type of auth required (0 = none)
\item \code{license_url} - License information
}
\item \code{created_at} - Feed creation timestamp
\item \code{external_ids} - External identifier information
\item \code{feed_contact_email} - Contact email for the feed
\item \code{official} - Whether feed is official
\item Additional metadata columns
}
}
\description{
Query the Mobility Database for transit feeds matching specified criteria.
Returns a tibble with feed metadata including download URLs.
}
\examples{
\dontrun{
# Get all active GTFS feeds in California
ca_feeds <- mobdb_feeds(
  country_code = "US",
  subdivision_name = "California",
  data_type = "gtfs",
  status = "active"
)

# Search for a specific provider
sf_muni <- mobdb_feeds(provider = "San Francisco")

# Get feeds with pagination
first_100 <- mobdb_feeds(limit = 100, offset = 0)
next_100 <- mobdb_feeds(limit = 100, offset = 100)
}

}
