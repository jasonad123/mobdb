% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{filter_by_validation}
\alias{filter_by_validation}
\title{Filter feeds or datasets by validation quality}
\usage{
filter_by_validation(
  data,
  max_errors = NULL,
  max_warnings = NULL,
  max_info = NULL,
  require_validation = TRUE
)
}
\arguments{
\item{data}{A tibble from \code{\link[=feeds]{feeds()}}, \code{\link[=mobdb_datasets]{mobdb_datasets()}}, or \code{\link[=mobdb_search]{mobdb_search()}}.}

\item{max_errors}{Maximum number of validation errors allowed. Use \code{0} for
error-free feeds. If \code{NULL} (default), no error filtering is applied.}

\item{max_warnings}{Maximum number of validation warnings allowed. If \code{NULL}
(default), no warning filtering is applied.}

\item{max_info}{Maximum number of informational notices allowed. If \code{NULL}
(default), no info filtering is applied.}

\item{require_validation}{Logical. If \code{TRUE} (default), exclude feeds/datasets
that have no validation data. If \code{FALSE}, include them in results.}
}
\value{
A filtered version of the input data frame containing only
feeds/datasets that meet the specified quality criteria.
}
\description{
Filter feed or dataset results by validation quality thresholds. This is a
convenience wrapper around \code{\link[=get_validation_report]{get_validation_report()}} that returns the original
data filtered to only include feeds/datasets meeting your quality criteria.
}
\examples{
\dontrun{
# Find all California feeds with zero errors
ca_feeds <- feeds(
  country_code = "US",
  subdivision_name = "California",
  data_type = "gtfs"
)
clean_feeds <- filter_by_validation(ca_feeds, max_errors = 0)

# Find feeds with minimal issues
quality_feeds <- filter_by_validation(
  ca_feeds,
  max_errors = 0,
  max_warnings = 100
)

# Get historical BART datasets with improving quality
bart <- feeds(provider = "Bay Area Rapid Transit")
datasets <- mobdb_datasets(bart$id[1], latest = FALSE)
improving <- filter_by_validation(datasets, max_errors = 5, max_warnings = 3000)
}
}
\seealso{
\code{\link[=get_validation_report]{get_validation_report()}} to inspect validation metrics,
\code{\link[=view_validation_report]{view_validation_report()}} to view full validation reports
}
\concept{validation}
