linters: linters_with_defaults(
    line_length_linter(NULL),
    commented_code_linter = NULL,
    pipe_consistency_linter = NULL,
    indentation_linter = NULL
  )
exclusions: list(
    "tests/testthat/",
    "R/integration-tidytransit.R" = list(
      object_usage_linter = Inf
    ) 
  )
