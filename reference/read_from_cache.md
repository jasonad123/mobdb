# Read from cache

Read from cache

## Usage

``` r
read_from_cache(cache_key, max_age = NULL)
```

## Arguments

- cache_key:

  Cache file name

- max_age:

  Maximum age in hours (NULL = no limit)

## Value

Cached data or NULL if not found/expired
